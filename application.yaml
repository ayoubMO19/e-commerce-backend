spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/vexadb
    username: admin
    password: vexa
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082
